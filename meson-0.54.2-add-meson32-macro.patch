diff -up meson-0.54.2/data/macros.meson.omv~ meson-0.54.2/data/macros.meson
--- meson-0.54.2/data/macros.meson.omv~	2020-05-18 18:05:23.002159835 +0200
+++ meson-0.54.2/data/macros.meson	2020-05-18 18:05:35.418309101 +0200
@@ -34,6 +34,30 @@
         %{_vpath_srcdir} %{_vpath_builddir} \
 	%{nil}}
 
+%meson32 \
+    %set_build_flags \
+    export LANG="en_US.UTF-8" \
+    export LC_ALL="en_US.UTF-8" \
+    PKG_CONFIG_PATH="%{_prefix}/lib/pkgconfig:%{_datadir}/pkgconfig:$PKG_CONFIG_PATH" CC=gcc CXX=g++ CFLAGS="`echo $CFLAGS |sed -e 's, -m64,,g;s, -mx32,,g;s, -flto,,g'` -m32" CXXFLAGS="`echo $CXXFLAGS |sed -e 's, -m64,,g;s, -mx32,,g;s, -flto,,g'` -m32" LDFLAGS="`echo $LDFLAGS |sed -e 's, -m64,,g;s, -mx32,,g;s, -flto,,g'` -m32" %{shrink:%{__meson} \
+        --buildtype=plain \
+        --prefix=%{_prefix} \
+        --libdir=%{_prefix}/lib \
+        --libexecdir=%{_libexecdir} \
+        --bindir=%{_bindir} \
+        --sbindir=%{_sbindir} \
+        --includedir=%{_includedir} \
+        --datadir=%{_datadir} \
+        --mandir=%{_mandir} \
+        --infodir=%{_infodir} \
+        --localedir=%{_datadir}/locale \
+        --sysconfdir=%{_sysconfdir} \
+        --localstatedir=%{_localstatedir} \
+        --sharedstatedir=%{_sharedstatedir} \
+        --wrap-mode=%{__meson_wrap_mode} \
+        --auto-features=%{__meson_auto_features} \
+        %{_vpath_srcdir} %{_vpath_builddir} \
+	%{nil}}
+
 %meson_build \
     export LC_ALL="en_US.UTF-8" \
     %ninja_build -C %{_vpath_builddir}
